package:
  name: django-pyodbc
  version: "0.3.0"

source:
  fn: django-pyodbc-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-pyodbc/django-pyodbc-0.3.0.tar.gz
  md5: 0b201282d9bf90694f0cc1cb98d413e7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-pyodbc = django-pyodbc:main
    #
    # Would create an entry point called django-pyodbc that calls django-pyodbc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyodbc >=3.0.6,<3.1

  run:
    - python
    - setuptools
    - pyodbc >=3.0.6,<3.1

test:
  # Python imports
  imports:
    - django_pyodbc
    - django_pyodbc.management
    - django_pyodbc.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  requires:
    # Put any additional test requirements here.  For example
    # - nose
    - python
    - setuptools
    - pyodbc >=3.0.6,<3.1

about:
  home: https://github.com/aurorasoftware/django-pyodbc
  license: See LICENSE
  summary: 'Django 1.5 SQL Server backend using pyodbc.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
