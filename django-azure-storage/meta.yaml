package:
  name: django-azure-storage
  version: "0.0.4"

source:
  fn: django-azure-storage-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-azure-storage/django-azure-storage-0.0.4.tar.gz
  md5: e0f973bec4e8207d1ab36458b786429d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-azure-storage = django-azure-storage:main
    #
    # Would create an entry point called django-azure-storage that calls django-azure-storage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - azure >=0.7.0,<1.1

  run:
    - python
    - django >=1.3
    - azure >=0.7.0,<1.1

test:
  # Python imports
  imports:
    - azure_storage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  requires:
    # Put any additional test requirements here.  For example
    # - nose
    - azure >=0.7.0,<1.1

about:
  home: https://github.com/Rediker-Software/django-azure-storage
  license: MIT License
  summary: 'Django storage backends for Windows Azure blob storage.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
