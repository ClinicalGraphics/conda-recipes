package:
  name: libgcrypt
  version: 1.7.3

source:
  fn: libgcrypt-1.7.3.tar.bz2
  url: https://gnupg.org/ftp/gcrypt/libgcrypt/libgcrypt-1.7.3.tar.bz2

build:
  number: 0
  has_prefix_files:
    - bin/libgcrypt-config
    - lib/libgcrypt.la

requirements:
  build:
    - libgpg-error

  run:
    - libgpg-error
