package:
  name: nine
  version: "0.3.4"

source:
  fn: nine-0.3.4.tar.gz
  url: https://pypi.python.org/packages/source/n/nine/nine-0.3.4.tar.gz
  md5: b3415655bba05e8a1a9caa3f8d9f9343
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nine = nine:main
    #
    # Would create an entry point called nine that calls nine.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - nine

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nandoflorestan/nine
  license: Public domain
  summary: 'Python 2 / 3 compatibility, like six, but favouring Python 3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
