package:
  name: asgi_redis
  version: 0.12.0

source:
  url: https://github.com/andrewgodwin/asgi_redis/archive/0.12.0.tar.gz
  fn: 0.12.0.tar.gz

build:
  number: 0
  noarch_python: True

requirements:
  build:
    - python
    - six
    - redis >=2.10
    - msgpack-python
    - asgiref >=0.13.0
  run:
    - python
    - six
    - redis >=2.10
    - msgpack-python
    - asgiref >=0.13.0

test:
  imports:
    - asgi_redis

about:
  home: https://github.com/andrewgodwin/asgi_redis
  license: BSD
  summary: Redis-backed ASGI channel layer implementation
