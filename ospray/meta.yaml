package:
  name: ospray
  version: 1.1.2

source:
  fn: ospray-1.1.2.x86_64.linux.tar.gz
  url: https://github.com/ospray/OSPRay/releases/download/v1.1.2/ospray-1.1.2.x86_64.linux.tar.gz

build:
  number: 1

requirements:
  build:
   - embree
   - tbb
  run:
   - embree
   - tbb

test:
  commands:
    - test -f $PREFIX/lib/libospray.so     # [linux]

about:
  home: http://www.ospray.org
  license: MIT + Others
