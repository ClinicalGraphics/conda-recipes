package:
  name: python-utils
  version: "1.6.2"

source:
  fn: python-utils-1.6.2.tar.gz
  url: https://pypi.python.org/packages/source/p/python-utils/python-utils-1.6.2.tar.gz
  md5: 5f85d7e8db6b539813d43bd9a2c60c82
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-utils = python-utils:main
    #
    # Would create an entry point called python-utils that calls python-utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - python_utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/WoLpH/python-utils
  license: BSD License
  summary: 'Python Utils is a module with some convenient utilities not included with the standard Python install'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
