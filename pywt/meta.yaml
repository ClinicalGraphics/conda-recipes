package:
  name: pywt
  version: "0.3.0"

source:
  git_tag: master
  git_url: https://github.com/PyWavelets/pywt.git
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
  # - pwt.main  

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - cython
    
  run:
    - python
    - setuptools
    - numpy
test:
  # Python imports
  imports:
    - pywt
    
  requires:
    - numpy
    - cython
    - flake8
    - nose
    

about:
  home: http://pywavelets.readthedocs.org/
  license: MIT License
  summary: 'PyWavelets - Discrete Wavelet Transforms in Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
