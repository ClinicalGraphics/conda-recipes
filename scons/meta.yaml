package:
  name: scons
  version: 2.5.1

source:
  fn: scons-2.5.1.tar.gz
  url: http://prdownloads.sourceforge.net/scons/scons-2.5.1.tar.gz

build:
  number: 0
  script: python setup.py install

requirements:
  build:
    - python <3

  run:
    - python <3

about:
  home: http://www.scons.org/
  license: MIT
  summary: Open Source next-generation build tool.
