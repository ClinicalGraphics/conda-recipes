package:
  name: pivottablejs
  version: "0.1.0"

source:
  fn: pivottablejs-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/p/pivottablejs/pivottablejs-0.1.0.tar.gz
  md5: f06492a38ab70b3b979eb2a908dd9261
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pivottablejs = pivottablejs:main
    #
    # Would create an entry point called pivottablejs that calls pivottablejs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - jupyter

test:
  # Python imports
  imports:
    - pivottablejs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nicolaskruchten/jupyter_pivottablejs
  license: MIT License
  summary: 'PivotTable.js integration for Jupyter/IPython Notebook'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
