package:
  name: nose-cover3
  version: "0.1.0"

source:
  fn: nose-cover3-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/n/nose-cover3/nose-cover3-0.1.0.tar.gz
  md5: 82f981eaa007b430679899256050fa0c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # noarch_python: True
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nose-cover3 = nose-cover3:main
    #
    # Would create an entry point called nose-cover3 that calls nose-cover3.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - nosecover3

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  requires:
    # Put any additional test requirements here.  For example
    - nose

about:
  home: http://github.com/ask/nosecover3
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Coverage 3.x support for Nose'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
