package:
  name: kombu
  version: 3.0.29

source:
  fn: kombu-3.0.29.tar.gz
  url: https://pypi.python.org/packages/source/k/kombu/kombu-3.0.29.tar.gz
  md5: 892bf89ee247c0d16d2bdd63f1ddf4c5

requirements:
  build:
  - python
  - setuptools
  - anyjson >=0.3.3
  - amqp >=1.4.7,<2.0

  run:
  - python
  - anyjson >=0.3.3
  - amqp >=1.4.7,<2.0

test:
  # Python imports
  imports:
    - kombu
    - kombu.async
    - kombu.tests
    - kombu.tests.async
    - kombu.tests.transport
    - kombu.tests.transport.virtual
    - kombu.tests.utils
    - kombu.transport
    - kombu.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://kombu.readthedocs.org
  license: BSD License
  summary: 'Messaging library for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
