package:
  name: pydicom
  version: 0.9.9

source:
  fn: pydicom-0.9.9.tar.gz
  url: https://github.com/darcymason/pydicom/archive/v0.9.9.tar.gz
  md5: 2248b21c63d220d16d32dd98a79f352c

build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  number: 0

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    #- numpy

test:
  # Python imports
  imports:
    - dicom

about:
  home: http://www.pydicom.org
  license: MIT License

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml