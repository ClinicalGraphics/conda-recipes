package:
  name: flake8-future-import
  version: "0.3.2"

source:
  fn: flake8-future-import-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/f/flake8-future-import/flake8-future-import-0.3.2.tar.gz
  md5: 3f5114ba4a8f4647cb36a1aecc71824b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # noarch_python: True
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flake8-future-import = flake8-future-import:main
    #
    # Would create an entry point called flake8-future-import that calls flake8-future-import.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flake8

  run:
    - python
    - setuptools
    - flake8

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/xZise/flake8-future-import
  license: MIT License
  summary: '__future__ import checker, plugin for flake8'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
