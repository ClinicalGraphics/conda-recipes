package:
  name: wfastcgi
  version: "2.2"

source:
  fn: wfastcgi-2.2.zip
  url: https://pypi.python.org/packages/source/w/wfastcgi/wfastcgi-2.2.zip
  md5: 2054ebf91d1a3601892bd4bf7b32f4d8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # noarch_python: True
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - wfastcgi = wfastcgi:main
    #
    # Would create an entry point called wfastcgi that calls wfastcgi.main()

    - wfastcgi = wfastcgi:main
    - wfastcgi-enable = wfastcgi:enable
    - wfastcgi-disable = wfastcgi:disable

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  # imports:

  #commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.
    ## These cause an infinite loop
    #- wfastcgi --help
    #- wfastcgi-enable --help
    #- wfastcgi-disable --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://aka.ms/PTVS
  license: Apache Software License
  summary: 'An IIS-Python bridge based on WSGI and FastCGI.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
