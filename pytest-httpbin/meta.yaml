package:
  name: pytest-httpbin
  version: "0.2.0"

source:
  fn: pytest-httpbin-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/p/pytest-httpbin/pytest-httpbin-0.2.0.tar.gz
  md5: 68734a9b674b03d9ac988701f0ee6072
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # noarch_python: True
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pytest-httpbin = pytest-httpbin:main
    #
    # Would create an entry point called pytest-httpbin that calls pytest-httpbin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - decorator
    - httpbin
    - six

  run:
    - python
    - setuptools
    - flask
    - decorator
    - httpbin
    - six

test:
  # Python imports
  imports:
    - pytest_httpbin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  requires:
    # Put any additional test requirements here.  For example
    - pytest

about:
  home: https://github.com/kevin1024/pytest-httpbin
  license: MIT License
  summary: 'Easily test your HTTP library against a local copy of httpbin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
