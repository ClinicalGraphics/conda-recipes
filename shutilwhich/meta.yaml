package:
  name: shutilwhich
  version: 1.1.0

source:
  fn: 1.1.0.tar.gz
  url: https://github.com/mbr/shutilwhich/archive/1.1.0.tar.gz

build:
  noarch_python: True
  number: 0
  script: python setup.py install

requirements:
  build:
    - python
  run:
    - python

test:
  imports:
    - shutilwhich

about:
  home: https://github.com/mbr/shutilwhich
  license: PSF
  summary: shutil.which for those not using Python 3.3 yet.
