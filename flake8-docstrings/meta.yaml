package:
  name: flake8-docstrings
  version: "0.2.5"

source:
  fn: flake8-docstrings-0.2.5.tar.gz
  url: https://github.com/PyCQA/flake8-docstrings/archive/0.2.5.tar.gz
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # noarch_python: True
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flake8-docstrings = flake8-docstrings:main
    #
    # Would create an entry point called flake8-docstrings that calls flake8-docstrings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flake8
    - pep257

  run:
    - python
    - flake8
    - pep257

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://gitlab.com/pycqa/flake8-docstrings
  license: MIT License
  summary: 'Extension for flake8 which uses pep257 to check docstrings'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
