package:
  name: fakeredis
  version: 0.8.1

source:
  fn: 0.8.1.tar.gz
  url: https://github.com/jamesls/fakeredis/archive/0.8.1.tar.gz

build:
  noarch_python: True
  number: 0
  script: python setup.py install

requirements:
  build:
    - python
    - pip
    - redis
  run:
    - python
    - redis

test:
  imports:
    - fakeredis

about:
  home: https://pypi.python.org/pypi/fakeredis
  license: BSD License